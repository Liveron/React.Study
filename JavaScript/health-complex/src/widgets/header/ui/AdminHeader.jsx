import AdminTitle from "widgets/header/ui/AdminTitle";
import HeaderLayout from "./HeaderLayout";

function AdminHeader() {
  return <HeaderLayout center={<AdminTitle />} />;
}

export default AdminHeader;
